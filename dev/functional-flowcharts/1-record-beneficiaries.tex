\documentclass{standalone}

\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}
\begin{document}
\pagestyle{empty}


% Define block styles
\tikzstyle{decision} = [diamond, draw, fill=blue!3, 
    text width=4.8em, text badly centered, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, fill=green!3, 
    text width=6em, text centered, rounded corners, minimum height=3.5em]
\tikzstyle{line} = [draw, very thick, -latex']
\tikzstyle{cloud} = [draw, ellipse,fill=red!3, node distance=2cm,
    minimum height=2em]
    
\begin{tikzpicture}[node distance =3cm, auto]
    % Place nodes
    \node [cloud] (start) {start};
    \node [block, below of=start,yshift=1.75cm] (listbens) {peruse list of beneficiaries};
    \node [decision, below of=listbens,yshift=.5cm] (inlist) {is needed beneficiary in list?};
    \node [block, left of=inlist] (addbentolist) {add beneficiary to list};
    \node [block, below of=inlist,yshift=.5cm] (recordben) {record beneficiary};
    \node [decision, below of=recordben,yshift=.5cm] (benneedrecorded) {record another beneficiary?};
    \node [cloud, below of=benneedrecorded] (done) {done};
    % Draw edges
    \path [line] (start) -- (listbens);
    \path [line] (listbens) -- (inlist);
    \path [line] (inlist) -- node {no}(addbentolist);
    \path [line] (addbentolist) -- (listbens);
    \path [line] (inlist) -- node {yes}(recordben);
    \path [line] (recordben) -- (benneedrecorded);
    \path [line] (benneedrecorded.west) --++ (-3.5cm,0cm) |- (listbens);
    \path [line] (benneedrecorded) -- node {no} (done);
\end{tikzpicture}

\end{document}